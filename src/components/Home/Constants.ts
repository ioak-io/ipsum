export const IPSUM_NUMBER_OF_BLOCKS = "IPSUM_NUMBER_OF_BLOCKS";

export const getCount = (index: number) => {
    return `IPSUM_COUNT_${index}`;
}

export const getType = (index: number) => {
    return `IPSUM_TYPE_${index}`;
}
